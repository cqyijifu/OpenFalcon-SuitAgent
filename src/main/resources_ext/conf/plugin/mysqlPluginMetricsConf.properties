################################################################################################
#				mysql 监控属性SQL语句配置
#				注:确保每一条语句只返回单行单列记录
################################################################################################

# 单个应用连接数据库的连接数大于指定值的数量
dbConnCountOverLimit = select count(*) from (select db,count(*) from information_schema.processlist group by db having count(*) > 200) a;

